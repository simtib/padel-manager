# Tournament domain

Tournament calculations are separated by responsibility here. Public callers should continue
importing from `src/utils/engine.ts`, which is the compatibility barrel for these modules.
